name: Issue 75 Apply

on:
  push:
    branches:
      - issue-75-jump-materialization-convergence

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check correction state
        id: gate
        shell: bash
        run: |
          if grep -q 'if (changed) stallDeadline = performance.now() + timeoutMs;' chatgpt-conversation-markdown-export.user.js; then
            echo 'needed=true' >> "$GITHUB_OUTPUT"
          else
            echo 'needed=false' >> "$GITHUB_OUTPUT"
          fi
      - name: Apply issue 75 scope correction
        if: steps.gate.outputs.needed == 'true'
        run: python3 tools/issue75-scope-fix.py
      - name: Verify focused correction
        if: steps.gate.outputs.needed == 'true'
        run: |
          node --check chatgpt-conversation-markdown-export.user.js
          node scripts/check-jsdoc.mjs
          node --test tests/jump-materialization.test.mjs
          git diff --check
          if git diff --exit-code --quiet; then
            echo 'Patch produced no changes.' >&2
            exit 1
          fi
      - name: Commit correction
        if: steps.gate.outputs.needed == 'true'
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add chatgpt-conversation-markdown-export.user.js tests/jump-materialization.test.mjs
          git commit -m 'fix(#75): preserve non-oldest Jump timeout'
          git push origin HEAD:issue-75-jump-materialization-convergence
